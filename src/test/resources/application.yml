spring:
  application:
    name: OmegaPoisk
    page-size: 5
  datasource:
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
  jackson:
    property-naming-strategy: SNAKE_CASE
jwt:
  secret: 'test'
server:
  servlet:
    context-path: '/api/v1'